import { createTheme } from "@mui/material/styles";

export const myTheme = createTheme({
  palette: {
    navbarColor: {
      main: "#3C3A3B",
      light: "#666666",
      dark: "#222222",
    },
    background: {
      myBackground: "#F4F4F4",
    },
  },
});
